# Best hyperparameters for AxisKT on nips2020_t34 from Optuna search
# (see runs/hyperparam_search/AxisKT_nips2020_t34_*_n_trials100/ for the full study).

experiment:
  model_name: AxisKT

data:
  dataset: nips2020_t34

model:
  hidden_dim: 64
  n_blocks: 4
  conv_kernel_size: 5
  conv_dilation_base: 2
  dropout: 0.298162789980157
  question_embed_dim: 16
  epochs: 100
  learning_rate: 0.00426304462555526
  weight_decay: 3.13596161098902e-05
  batch_size: 32
  max_clip_grad_norm: 8.028476659458729
