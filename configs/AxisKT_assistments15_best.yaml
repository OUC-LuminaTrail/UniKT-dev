# Best hyperparameters for AxisKT on assistments15 from Optuna search
# (see runs/hyperparam_search/AxisKT_assistments15_*_n_trials100/ for the full study).

experiment:
  model_name: AxisKT

data:
  dataset: assistments15

model:
  hidden_dim: 128
  n_blocks: 4
  conv_kernel_size: 7
  conv_dilation_base: 2
  dropout: 0.4707523405113597
  question_embed_dim: 64
  epochs: 100
  learning_rate: 0.0015382689274387124
  weight_decay: 2.6600444065068465e-05
  batch_size: 64
  max_clip_grad_norm: 0.759234023157446
