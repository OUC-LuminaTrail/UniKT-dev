# Best hyperparameters for AxisKT on assistments12 from Optuna search
# (see runs/hyperparam_search/AxisKT_assistments12_*_n_trials100/ for the full study).

experiment:
  model_name: AxisKT

data:
  dataset: assistments12

model:
  hidden_dim: 128
  n_blocks: 4
  conv_kernel_size: 7
  conv_dilation_base: 2
  dropout: 0.06628035643239341
  question_embed_dim: 256
  epochs: 100
  learning_rate: 0.0030459646122261777
  weight_decay: 1.0170758446504375e-05
  batch_size: 64
  max_clip_grad_norm: 0.8405309741917287
