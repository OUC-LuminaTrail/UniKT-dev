"""Router package for the web API.

All endpoint modules (capabilities, datasets, environments, gpu, logs,
preprocess, resource, schemas_api, settings_api, tasks) are imported from
other files in this package and registered in ``web.backend.main``.
"""
