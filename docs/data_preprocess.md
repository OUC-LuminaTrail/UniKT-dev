# 数据处理说明

## 数据映射
为了提供统一的数据处理逻辑，在读取数据后，对原始数据中的列名进行如下映射。表中给出的是映射后的列名。

- 用户ID：`user`
- 问题ID：`question`
- 技能ID：`skill`
- 问题模板ID：`template`
- 作业/课程ID：`assignment`
- 问题正确与否：`label`

对于ID型数据，均已将原始数据重新编码为连续的整数（从`0`开始编码），便于在模型中使用动态的嵌入层，同时避免非连续ID在图构建中可能引发的数据越界问题。
